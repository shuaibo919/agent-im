// Preload script — exposes nothing to renderer for security
// The web UI communicates with the server via fetch (same as browser mode)
window.addEventListener('DOMContentLoaded', () => {
  console.log('Agent-IM Desktop loaded')
})
